<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Birthday Message</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <canvas id="confetti" style="display: <%= showConfetti ? 'block' : 'none' %>; "></canvas>
    <div class="container">
        <h1><%= message %></h1>
        <p><%= subtext %></p>
        <a href="/" class="btn">Go Back</a>
    </div>

    <% if (showConfetti) { %>
    <script src="/confetti.js"></script>
    <% } %>
</body>
</html>
